[package]
name = "rekha-storage"
description = "Catalog (redb) and object-store (local fs) traits + implementations for RekhaDB."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rekha-core = { path = "../rekha-core" }
redb = "4"
bincode = "1"
serde.workspace = true
serde_json.workspace = true
uuid.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
