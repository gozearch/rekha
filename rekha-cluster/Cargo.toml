[package]
name = "rekha-cluster"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Cluster membership and peer communication for Rekha"
keywords = ["vector-database", "cluster", "membership"]
categories = ["database"]

[dependencies]
rekha-core = { path = "../rekha-core" }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
dashmap = "6"

[dev-dependencies]
tempfile = "3"
